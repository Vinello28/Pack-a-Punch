

# Core dependencies
#torch
transformers<4.45.0
tokenizers
pydantic
pydantic-settings

# Triton client (inference gateway -> Triton server over gRPC)
tritonclient[grpc]

# ONNX export only (offline, not needed by the gateway at runtime):
# install torch + optimum[onnxruntime-gpu] to run scripts/export_triton.py
# onnxruntime-gpu
# tensorrt
# optimum[onnxruntime-gpu]


# API Server
fastapi
uvicorn[standard]
python-multipart

# Training
datasets
accelerate
scikit-learn

# HTTP Client (for distillation)
httpx

# Utilities
numpy
tqdm
loguru
pyyaml

# Testing
pytest
pytest-asyncio

# Development
black
ruff
